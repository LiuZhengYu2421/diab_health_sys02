# 智糖健康管理平台（Vue 3）· 前端对接说明

> **本文档用途：前端部署、启动、Mock/真实接口切换、认证接口对接约定。**
> 后端全部接口规范见同目录 `后端接口文档.md`。
> AI 功能对接 Dify 平台的完整说明（后端代理接口、Dify 应用配置）见同目录 `Dify对接文档.md`。

本项目已由原生 HTML 改造为 **Vue 3 + Vite + Pinia + Vue Router + Axios** 项目，
登录/注册已**完全脱离 Dify**，改为对接自建后端（SpringBoot）；
**AI 能力**（医师咨询 / 智能助手 / 打卡分析 / 风险预测 / AI 数据助理 / 健康资讯 / 方案定制）仍通过后端代理接口对接 **Dify 平台**。

---

## 一、快速启动

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器（默认 http://localhost:5173）
npm run dev

# 3. 生产构建
npm run build
```

> 默认 `VITE_USE_MOCK=true`，使用本地 Mock 数据，无需后端即可运行演示。
> 登录/注册任意新用户名即可注册，已注册用户可直接登录。

---

## 二、Mock / 真实接口 切换

通过环境变量控制（`.env.development` / `.env.production`）：

| 变量 | 值 | 说明 |
| --- | --- | --- |
| `VITE_USE_MOCK` | `true` | 本地 Mock 模式（localStorage 模拟用户库） |
| `VITE_USE_MOCK` | `false` | 请求真实 SpringBoot 接口 |
| `VITE_API_BASE_URL` | `/api` | 接口前缀 |
| `VITE_API_SERVER_URL` | 空 | 后端完整地址；留空时走 `VITE_API_BASE_URL` + Vite 代理 |

**对接真实后端的步骤：**
1. 启动 SpringBoot 项目（默认端口 `8080`，可通过 `server.port` 修改）。
2. 将 `.env.development` 中 `VITE_USE_MOCK` 改为 `false`。
3. 若后端端口不是 `8080`，同步修改 `vite.config.js` 中 `server.proxy['/api'].target`。
4. 重启 `npm run dev` 即可。

---

## 三、接口约定（请在 SpringBoot 中实现）

前端调用文件：
- 认证接口：`src/api/auth.js`
- 业务接口：`src/api/admin.js`、`src/api/punchIn.js`
- Dify 接口：`src/api/dify.js`
- 请求封装：`src/api/request.js`

### 1. 统一响应格式

```json
{
  "code": 200,
  "message": "ok",
  "data": {}
}
```

- `code`：`200` 或 `0` 表示成功；`401` 表示未登录/登录过期（前端自动清理并跳转登录页）。
- `data`：业务数据。

### 2. 接口清单

**认证相关（6 个）：**

| 方法 | 路径 | 请求体 | 成功返回 data | 说明 |
| --- | --- | --- | --- | --- |
| POST | `/api/auth/login` | `{ "username": "xxx", "password": "xxx" }` | `{ "token": "jwt...", "userInfo": {...} }` | 登录 |
| POST | `/api/auth/register` | `{ "username": "xxx", "password": "xxx", "nickname": "xxx" }` | `{ "token": "jwt...", "userInfo": {...} }` | 注册 |
| POST | `/api/auth/logout` | 无（携带 token） | `{}` | 退出登录 |
| GET | `/api/user/info` | 无（携带 token） | `{ ...userInfo }` | 获取当前用户信息 |
| PUT | `/api/user/info` | `{ "nickname": "xxx", "desc": "xxx", "avatar": "xxx" }` | `{ ...userInfo }` | 更新用户资料 |
| PUT | `/api/user/password` | `{ "oldPassword": "xxx", "newPassword": "xxx" }` | `{}` | 修改密码 |

**Dify 代理接口（8 个，前端统一走 `src/api/dify.js`）：**

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/dify/doctor/chat` | 医师咨询对话 |
| POST | `/api/dify/punch/analyze` | 智能打卡分析 |
| POST | `/api/dify/risk/predict` | 糖尿病风险预测 |
| POST | `/api/dify/assistant/chat` | 智能助手对话 |
| POST | `/api/dify/admin/query` | AI 数据助理（管理员） |
| POST | `/api/dify/health/tags` | 健康资讯标签模式 |
| POST | `/api/dify/health/detail` | 健康资讯详情模式 |
| POST | `/api/dify/life/scheme` | 方案定制生成 |

> 以上 8 个接口的**请求体 / 响应体**在 `src/api/dify.js` 文件头注释中有完整约定；后端负责调用 Dify 平台并转换格式，详见 `Dify对接文档.md`。

**首页搜索接口（SpringBoot 业务接口，非 Dify，复用文章列表接口）：**

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/articles?keyword=xxx&pageSize=8` | 健康咨询搜索（按标题模糊查询 `articles` 表，`TeamView.vue` 首页搜索） |

> 前端封装在 `src/api/search.js`，见《后端接口文档》11.1。

> 其余业务接口（医生/文章/收藏/风险/方案/建议/打卡/用户管理，共 42 个）见项目根目录 `后端接口文档.md`。

### 3. userInfo 建议字段

```json
{
  "id": 1,
  "username": "zhang",
  "nickname": "老张",
  "avatar": "/img/user_icon.png",
  "desc": "智慧控糖 · 健康生活",
  "role": "user",
  "createdAt": "2026-08-12"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `role` | string | `user`（默认）/ `doctor` / `admin`，前端据此控制权限入口 |

> 前端展示优先级：`nickname > username > name > userName`；`avatar` 留空时使用默认头像。

### 4. Token 传递

前端会在每次请求的请求头自动携带：

```
Authorization: Bearer <token>
```

SpringBoot 建议使用 JWT 或 Session 校验，`401` 时返回约定格式。

---

## 四、前端接口封装（如需新增业务接口）

`src/api/request.js` 已封装 axios 实例（自动带 token、统一处理错误码），
新增接口时在 `src/api/` 下新建模块即可：

```js
// src/api/health.js
import request from './request'

// 获取健康档案
export function getHealthProfile() {
  return request.get('/user/health-profile')
}

// 提交打卡
export function submitCheckin(data) {
  return request.post('/checkin', data)
}
```

**新增 Dify 接口**时请复用 `src/api/dify.js` 的双模式结构（Mock / 真实代理）：

```js
// 在 dify.js 中追加（约定 Mock 返回与真实返回的 data 结构一致）
export function someAiFeature(data) {
  if (USE_MOCK) return mockSomeAiFeature(data)
  return request.post('/dify/some-feature', data)
}
```

> 注意：Dify 接口的 Mock 返回（`res`）与真实返回（`res.data`）结构不同，调用处统一用
> `const data = isMockMode() ? res : (res.data || res)` 取数。

---

## 五、目录结构

```
├── index.html                  # Vite 入口（挂载 #app）
├── vite.config.js              # Vite 配置 + /api 代理到 SpringBoot
├── .env.development            # 开发环境变量（Mock 开关 / 接口地址）
├── .env.production             # 生产环境变量
├── public/                     # 静态资源（css / img / webfonts）
└── src/
    ├── main.js                 # 应用入口
    ├── App.vue                 # 根组件
    ├── router/index.js         # 路由 + 登录守卫
    ├── stores/user.js          # Pinia 用户状态（userInfo / token）
    ├── api/
    │   ├── request.js          # Axios 封装（token / 错误处理）
    │   ├── auth.js             # 登录/注册/退出/用户信息（SpringBoot 对接点）
    │   ├── admin.js            # 管理端接口（用户/医生/文章/种类）
    │   ├── punchIn.js          # 打卡相关接口
    │   └── dify.js             # Dify 代理接口（8 个 AI 功能，Mock/真实双模式）
    ├── utils/
    │   ├── storage.js          # token / 用户信息本地存储
    │   ├── token.js            # token 读写
    │   ├── alert.js            # 浮动提示
    │   └── operationLog.js     # AI 功能运维日志（写 localStorage，管理端查看）
    └── views/
        ├── LoginView.vue       # 登录/注册页
        ├── LayoutView.vue      # 主框架（顶部导航 + 内容区，无侧栏）
        ├── TeamView.vue        # 首页（Banner + 医师团队 + 健康咨询 + 全局搜索）
        ├── DoctorConsultView.vue # 医师 AI 在线咨询（Dify 对话）
        ├── AiAssistantView.vue # 智能助手（Dify 对话）
        ├── HealthConsultView.vue # 健康咨询（列表 + 详情收藏；支持 ?open=标题 直达详情；收藏入个人中心「我的咨询」）
        ├── LifeSchemeView.vue  # 方案定制（暂无方案 → 表单定制 → Dify 生成方案）
        ├── PunchAnalyzeView.vue # 智能打卡分析（Dify）
        ├── RiskPredictView.vue # 糖尿病风险预测（Dify）
        ├── MineView.vue        # 个人中心（打卡 / 方案 / 建议）
        ├── AdminView.vue       # 管理后台（路由守卫 role=admin）
        ├── admin/              # 管理后台面板
        │   ├── DashboardPanel.vue        # 数据概览
        │   ├── UserPanel.vue             # 用户管理
        │   ├── DoctorPanel.vue           # 医生管理（含 Dify chatToken 配置）
        │   ├── ArticlePanel.vue          # 文章管理
        │   ├── PendingPanel.vue          # 待办/审核
        │   ├── AiDataAssistantPanel.vue  # AI 数据助理（Dify 工作流 + 数据库）
        │   ├── OperationLogPanel.vue     # AI 功能运维日志
        │   └── panel-style.css           # 面板公共样式
        └── PlaceholderView.vue # 建设中占位页
```

### AI 功能页面与 Dify 接口对应关系

| 页面 | Dify 接口 | Dify 应用类型 |
| --- | --- | --- |
| 医师 AI 在线咨询 `DoctorConsultView` | `/dify/doctor/chat` | 对话型（每医生一个应用，`doctor_information.chatToken`） |
| 智能助手 `AiAssistantView` | `/dify/assistant/chat` | 对话型 |
| 智能打卡分析 `PunchAnalyzeView` | `/dify/punch/analyze` | 工作流型 |
| 糖尿病风险预测 `RiskPredictView` | `/dify/risk/predict` | 工作流型 |
| AI 数据助理（后台）`AiDataAssistantPanel` | `/dify/admin/query` | 工作流型（连接数据库） |
| 健康咨询 `HealthConsultView` | `/dify/health/tags`、`/dify/health/detail` | 工作流型 |
| 方案定制 `LifeSchemeView` / 个人中心建议 | `/dify/life/scheme` | 工作流型 |
