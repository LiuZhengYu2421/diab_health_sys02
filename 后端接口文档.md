# 智糖健康管理平台 - 后端接口文档

> **本文档用途：后端（SpringBoot）接口规范，包含全部 42 个接口的定义。**
> 前端启动 / 部署 / 环境配置见同目录 `README-前端对接说明.md`。
> 数据库建表脚本见同目录 `schema-mysql.sql`。

> 数据库：MySQL（对应 `schema-mysql.sql`）
> 基础路径：开发环境 `/api`（Vite 代理到 `http://localhost:8080`）
> 认证方式：请求头 `Authorization: Bearer <token>`（登录/注册后获取）

---

## 一、通用约定

### 1.1 统一响应格式

所有接口返回以下 JSON 结构：

```json
{
  "code": 200,
  "message": "ok",
  "data": {}
}
```

| code | 含义 | 前端处理 |
| --- | --- | --- |
| 200 或 0 | 成功 | 返回 `data` |
| 400 | 参数错误 | 提示 `message` |
| 401 | 未登录 / token 过期 | 自动清 token 并跳登录页 |
| 403 | 无权限 | 提示「没有权限执行该操作」 |
| 404 | 资源不存在 | 提示 `message` |
| 500 | 服务器异常 | 提示「服务器异常，请稍后再试」 |

### 1.2 分页参数

| 参数 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `page` | int | 1 | 页码，从 1 开始 |
| `pageSize` | int | 10 | 每页条数 |

分页响应统一返回：

```json
{
  "list": [],
  "total": 100,
  "page": 1,
  "pageSize": 10
}
```

### 1.3 时间格式

所有时间字段统一为 `yyyy-MM-dd HH:mm:ss`（MySQL `DATETIME`）。

---

## 二、认证模块（表 `users`）

> 用户角色由 `users.role` 字段区分：`user`（普通用户，注册默认）/ `doctor`（医生）/ `admin`（管理员）。
> 前端根据 `userInfo.role` 控制管理入口和按钮显隐；管理端接口要求 `role = admin`。

### 2.1 用户登录

- **POST** `/auth/login`
- **无需登录**

请求体：

```json
{ "username": "admin", "password": "admin123" }
```

成功响应 `data`：

```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "userInfo": {
    "id": 1,
    "username": "admin",
    "nickname": "演示用户",
    "avatar": "/img/user_icon.png",
    "desc": "智慧控糖 · 健康生活",
    "role": "admin",
    "createdAt": "2026-08-12"
  }
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `role` | string | `user` / `doctor` / `admin`，注册默认 `user` |

| 错误码 | 场景 |
| --- | --- |
| 400 | 用户名或密码为空 |
| 401 | 用户名或密码错误 |

### 2.2 用户注册

- **POST** `/auth/register`
- **无需登录**

请求体：

```json
{ "username": "zhangsan", "password": "123456", "nickname": "张三" }
```

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 3~20 位，唯一 |
| `password` | string | 是 | 至少 6 位 |
| `nickname` | string | 否 | 默认取 username |

成功响应 `data`：同登录，返回 `{ token, userInfo }`，其中 `role` 固定为 `user`。

| 错误码 | 场景 |
| --- | --- |
| 400 | 参数不合法（用户名过短、密码不足 6 位） |
| 409 | 用户名已存在 |

### 2.3 退出登录

- **POST** `/auth/logout`
- **需登录**（携带 token）

成功响应 `data`：`{}`

### 2.4 获取当前用户信息

- **GET** `/user/info`
- **需登录**

成功响应 `data`：

```json
{
  "id": 1,
  "username": "admin",
  "nickname": "演示用户",
  "avatar": "/img/user_icon.png",
  "desc": "智慧控糖 · 健康生活",
  "role": "admin",
  "createdAt": "2026-08-12"
}
```

> 前端 `userStore` 兼容两种返回：`{ userInfo: {...} }` 或直接 `{...}`，二选一即可。
> 昵称展示优先级：`nickname > username`；头像字段兼容 `avatar` / `avatarUrl`。
> `role` 由服务端从 token 对应的用户读取，前端不可修改。

### 2.5 更新个人信息

- **PUT** `/user/info`
- **需登录**

请求体（只需传要修改的字段）：

```json
{
  "nickname": "新昵称",
  "avatar": "/img/new_avatar.png",
  "desc": "我的控糖宣言"
}
```

成功响应 `data`：更新后的 `userInfo`。

> `role` 不允许用户自行修改，服务端忽略该字段。角色变更仅由管理员操作数据库或专门接口完成。

### 2.6 修改密码

- **PUT** `/user/password`
- **需登录**

请求体：

```json
{ "oldPassword": "123456", "newPassword": "654321" }
```

| 错误码 | 场景 |
| --- | --- |
| 400 | 新密码不足 6 位 |
| 401 | 原密码错误 |

---

## 三、医生资讯模块（表 `doctor_information`）

> 说明：医生**列表与资料管理**走普通 CRUD；「立即咨询」的在线对话部分对接 Dify，见 Dify 对接文档（另行提供）。

### 3.1 医生列表（分页 + 搜索）

- **GET** `/doctors`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `page` | int | 否 | 默认 1 |
| `pageSize` | int | 否 | 默认 10 |
| `keyword` | string | 否 | 按医生姓名模糊搜索 |
| `department` | string | 否 | 按科室精确筛选 |

成功响应 `data`：

```json
{
  "list": [
    {
      "infoId": 1,
      "doctorName": "李建华",
      "department": "内分泌科",
      "title": "主任医师",
      "introduction": "从事糖尿病临床诊疗 25 年...",
      "imageUrl": "/img/doc1.jpg",
      "chatToken": ""
    }
  ],
  "total": 12,
  "page": 1,
  "pageSize": 10
}
```

> `chatToken` 为 Dify 对话所需，未配置时返回空字符串，前端据此决定是否展示「立即咨询」按钮。

### 3.2 医生详情

- **GET** `/doctors/{infoId}`

成功响应 `data`：单个医生对象（字段同上）。

### 3.3 新增医生（管理端）

- **POST** `/admin/doctors`
- **需管理员权限**（由 `RoleInterceptor` 校验，非 admin 返回 403）

请求体：

```json
{
  "doctorName": "李建华",
  "department": "内分泌科",
  "title": "主任医师",
  "introduction": "简介内容",
  "imageUrl": "/img/doc1.jpg",
  "chatToken": ""
}
```

成功响应 `data`：`{ "infoId": 13 }`

### 3.4 更新医生（管理端）

- **PUT** `/admin/doctors/{infoId}`
- **需管理员权限**

请求体：同 3.3，字段可选。成功响应 `data`：`{}`

### 3.5 删除医生（管理端）

- **DELETE** `/admin/doctors/{infoId}`
- **需管理员权限**

成功响应 `data`：`{}`

---

## 四、文章科普模块（表 `articles`）

### 4.1 文章列表（分页 + 分类 + 搜索）

- **GET** `/articles`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `page` | int | 否 | 默认 1 |
| `pageSize` | int | 否 | 默认 10 |
| `category` | string | 否 | 分类（饮食/运动/监测/用药/并发症…） |
| `keyword` | string | 否 | 按标题模糊搜索 |

成功响应 `data`：

```json
{
  "list": [
    {
      "articleId": 1,
      "title": "糖尿病患者秋季饮食指南",
      "coverUrl": "/img/a1.jpg",
      "author": "健康管理师",
      "publishTime": "2026-08-10 09:00:00",
      "content": "文章正文（Markdown 或富文本）",
      "category": "饮食",
      "views": 1200
    }
  ],
  "total": 8,
  "page": 1,
  "pageSize": 10
}
```

### 4.2 文章详情（浏览量 +1）

- **GET** `/articles/{articleId}`

成功响应 `data`：单个文章对象，且服务端 `views` 自动 +1。

### 4.3 新增文章（管理端）

- **POST** `/admin/articles`
- **需管理员权限**（由 `RoleInterceptor` 校验，非 admin 返回 403）

请求体：

```json
{
  "title": "文章标题",
  "coverUrl": "/img/a1.jpg",
  "author": "作者名",
  "publishTime": "2026-08-12 10:00:00",
  "content": "正文内容",
  "category": "饮食"
}
```

成功响应 `data`：`{ "articleId": 9 }`

### 4.4 更新文章（管理端）

- **PUT** `/admin/articles/{articleId}`
- **需管理员权限**

请求体：同 4.3，字段可选。成功响应 `data`：`{}`

### 4.5 删除文章（管理端）

- **DELETE** `/admin/articles/{articleId}`
- **需管理员权限**

成功响应 `data`：`{}`

### 4.6 获取文章分类列表

- **GET** `/articles/categories`

成功响应 `data`：`["饮食", "运动", "监测", "用药", "并发症"]`

---

## 五、文章收藏模块（表 `article_collections`）

### 5.1 收藏文章

- **POST** `/articles/{articleId}/favorite`
- **需登录**

成功响应 `data`：`{}`

> 已收藏时重复调用不报错（幂等），返回成功。

### 5.2 取消收藏

- **DELETE** `/articles/{articleId}/favorite`
- **需登录**

成功响应 `data`：`{}`

### 5.3 我的收藏列表

- **GET** `/articles/favorites`
- **需登录**

请求参数：`page`、`pageSize`（可选）。

成功响应 `data`：

```json
{
  "list": [
    {
      "collectionId": 1,
      "articleId": 1,
      "title": "文章标题",
      "coverUrl": "/img/a1.jpg",
      "author": "健康管理师",
      "publishTime": "2026-08-10 09:00:00",
      "category": "饮食",
      "views": 1200
    }
  ],
  "total": 3,
  "page": 1,
  "pageSize": 10
}
```

### 5.4 查询是否已收藏

- **GET** `/articles/{articleId}/favorite/status`
- **需登录**

成功响应 `data`：`{ "favorited": true }`

---

## 六、糖尿病种类模块（表 `diabetes_types`）

### 6.1 种类列表

- **GET** `/diabetes-types`

成功响应 `data`：

```json
{
  "list": [
    {
      "typeId": 1,
      "typeName": "1型糖尿病",
      "img": "/img/t1.jpg",
      "pathogenesis": "发病机制...",
      "manifestation": "临床表现...",
      "treatment": "治疗方案..."
    }
  ],
  "total": 5,
  "page": 1,
  "pageSize": 10
}
```

### 6.2 种类详情

- **GET** `/diabetes-types/{typeId}`

成功响应 `data`：单个种类对象。

### 6.3 新增种类（管理端）

- **POST** `/admin/diabetes-types`
- **需管理员权限**（由 `RoleInterceptor` 校验，非 admin 返回 403）

请求体：`typeName`（必填）、`img`、`pathogenesis`、`manifestation`、`treatment`。

成功响应 `data`：`{ "typeId": 6 }`

### 6.4 更新种类（管理端）

- **PUT** `/admin/diabetes-types/{typeId}`
- **需管理员权限**

请求体：字段可选。成功响应 `data`：`{}`

### 6.5 删除种类（管理端）

- **DELETE** `/admin/diabetes-types/{typeId}`
- **需管理员权限**

成功响应 `data`：`{}`

---

## 七、用户风险信息模块（表 `user_risk_info`）

> 该表用于风险测评，一个用户对应一条记录。字段为前端已定死的驼峰命名，后端返回时保持一致。

### 7.1 获取我的风险信息

- **GET** `/risk-info`
- **需登录**

成功响应 `data`：

```json
{
  "userId": 1,
  "age": 45,
  "sex": "男",
  "height": 172,
  "weight": 68,
  "familyHistory": "无",
  "waistline": 85.5,
  "systolicPressure": 120,
  "isPregnancy": "否",
  "message": "综合评估结果描述",
  "disease": "2型糖尿病"
}
```

未填写过时返回 `data: null`。

### 7.2 提交 / 更新风险信息

- **POST** `/risk-info`
- **需登录**

请求体：

```json
{
  "age": 45,
  "sex": "男",
  "height": 172,
  "weight": 68,
  "familyHistory": "无",
  "waistline": 85.5,
  "systolicPressure": 120,
  "isPregnancy": "否",
  "message": "综合评估结果描述",
  "disease": "2型糖尿病"
}
```

> 逻辑：当前用户已有记录则覆盖更新，没有则新增。

成功响应 `data`：`{ "userId": 1 }`

---

## 八、生活方案模块（表 `life_plans`）

### 8.1 我的方案列表

- **GET** `/life-plans`
- **需登录**

请求参数：`page`、`pageSize`（可选）；可加 `type`（饮食/运动/其他）筛选。

成功响应 `data`：

```json
{
  "list": [
    {
      "id": 1,
      "userId": 1,
      "type": "饮食",
      "order": 1,
      "time": "08:30",
      "title": "低GI早餐搭配",
      "content": "燕麦 50g + 鸡蛋 1 个 + 无糖豆浆 200ml"
    }
  ],
  "total": 12,
  "page": 1,
  "pageSize": 10
}
```

### 8.2 方案详情

- **GET** `/life-plans/{id}`
- **需登录（只能查自己的）**

成功响应 `data`：单个方案对象。

### 8.3 新增方案

- **POST** `/life-plans`
- **需登录**

请求体：

```json
{
  "type": "饮食",
  "order": 1,
  "time": "08:30",
  "title": "低GI早餐搭配",
  "content": "燕麦 50g + 鸡蛋 1 个"
}
```

> `user_id` 由服务端从 token 解析，前端不传。
> `type` 只能为 `饮食 / 运动 / 其他`。

成功响应 `data`：`{ "id": 13 }`

### 8.4 更新方案

- **PUT** `/life-plans/{id}`
- **需登录（只能改自己的）**

请求体：字段可选。成功响应 `data`：`{}`

### 8.5 删除方案

- **DELETE** `/life-plans/{id}`
- **需登录（只能删自己的）**

成功响应 `data`：`{}`

---

## 九、生活建议模块（表 `life_advice`）

### 9.1 我的建议列表

- **GET** `/life-advice`
- **需登录**

请求参数：`page`、`pageSize`（可选）。

成功响应 `data`：

```json
{
  "list": [
    {
      "id": 1,
      "userId": 1,
      "title": "糖尿病患者秋季饮食指南",
      "tags": "饮食,秋季",
      "content": "建议正文..."
    }
  ],
  "total": 3,
  "page": 1,
  "pageSize": 10
}
```

### 9.2 建议详情

- **GET** `/life-advice/{id}`
- **需登录**

成功响应 `data`：单个建议对象。

### 9.3 新增建议

- **POST** `/life-advice`
- **需登录**

请求体：

```json
{
  "title": "建议标题",
  "tags": "饮食,秋季",
  "content": "建议正文"
}
```

成功响应 `data`：`{ "id": 4 }`

### 9.4 更新建议

- **PUT** `/life-advice/{id}`
- **需登录**

请求体：字段可选。成功响应 `data`：`{}`

### 9.5 删除建议

- **DELETE** `/life-advice/{id}`
- **需登录**

成功响应 `data`：`{}`

---

## 十、打卡记录模块（表 `punch_in`）

### 10.1 新增打卡

- **POST** `/punch-in`
- **需登录**

请求体：

```json
{
  "punchType": "血糖监测",
  "completionStatus": "已完成",
  "message": "空腹血糖 5.6 mmol/L"
}
```

> `punch_time` 由服务端生成当前时间；`user_id` 从 token 解析。

成功响应 `data`：

```json
{
  "id": 1,
  "userId": 1,
  "punchTime": "2026-08-12 08:30:00",
  "punchType": "血糖监测",
  "completionStatus": "已完成",
  "message": "空腹血糖 5.6 mmol/L"
}
```

### 10.2 我的打卡记录列表

- **GET** `/punch-in`
- **需登录**

请求参数：`page`、`pageSize`（可选）；`punchType`（可选，按类型筛选）；`startDate` / `endDate`（可选，按日期范围筛选，格式 `yyyy-MM-dd`）。

成功响应 `data`：

```json
{
  "list": [
    {
      "id": 1,
      "userId": 1,
      "punchTime": "2026-08-12 08:30:00",
      "punchType": "血糖监测",
      "completionStatus": "已完成",
      "message": "空腹血糖 5.6 mmol/L"
    }
  ],
  "total": 26,
  "page": 1,
  "pageSize": 10
}
```

### 10.3 打卡统计

- **GET** `/punch-in/stats`
- **需登录**

成功响应 `data`：

```json
{
  "streak": 12,
  "monthCount": 26,
  "totalCount": 180
}
```

| 字段 | 说明 |
| --- | --- |
| `streak` | 连续打卡天数 |
| `monthCount` | 本月打卡次数 |
| `totalCount` | 累计打卡次数 |

### 10.4 删除打卡记录

- **DELETE** `/punch-in/{id}`
- **需登录（只能删自己的）**

成功响应 `data`：`{}`

---

## 附录：接口总览

| # | 方法 | 路径 | 说明 | 登录 |
| --- | --- | --- | --- | --- |
| 1 | POST | `/auth/login` | 登录 | 否 |
| 2 | POST | `/auth/register` | 注册 | 否 |
| 3 | POST | `/auth/logout` | 退出登录 | 是 |
| 4 | GET | `/user/info` | 获取用户信息 | 是 |
| 5 | PUT | `/user/info` | 更新个人信息 | 是 |
| 6 | PUT | `/user/password` | 修改密码 | 是 |
| 7 | GET | `/doctors` | 医生列表 | 否* |
| 8 | GET | `/doctors/{id}` | 医生详情 | 否* |
| 9 | POST | `/admin/doctors` | 新增医生 | 管理员 |
| 10 | PUT | `/admin/doctors/{id}` | 更新医生 | 管理员 |
| 11 | DELETE | `/admin/doctors/{id}` | 删除医生 | 管理员 |
| 12 | GET | `/articles` | 文章列表 | 否* |
| 13 | GET | `/articles/{id}` | 文章详情 | 否* |
| 14 | GET | `/articles/categories` | 文章分类 | 否* |
| 15 | POST | `/admin/articles` | 新增文章 | 管理员 |
| 16 | PUT | `/admin/articles/{id}` | 更新文章 | 管理员 |
| 17 | DELETE | `/admin/articles/{id}` | 删除文章 | 管理员 |
| 18 | POST | `/articles/{id}/favorite` | 收藏文章 | 是 |
| 19 | DELETE | `/articles/{id}/favorite` | 取消收藏 | 是 |
| 20 | GET | `/articles/favorites` | 我的收藏列表 | 是 |
| 21 | GET | `/articles/{id}/favorite/status` | 是否已收藏 | 是 |
| 22 | GET | `/diabetes-types` | 种类列表 | 否* |
| 23 | GET | `/diabetes-types/{id}` | 种类详情 | 否* |
| 24 | POST | `/admin/diabetes-types` | 新增种类 | 管理员 |
| 25 | PUT | `/admin/diabetes-types/{id}` | 更新种类 | 管理员 |
| 26 | DELETE | `/admin/diabetes-types/{id}` | 删除种类 | 管理员 |
| 27 | GET | `/risk-info` | 我的风险信息 | 是 |
| 28 | POST | `/risk-info` | 提交/更新风险信息 | 是 |
| 29 | GET | `/life-plans` | 我的方案列表 | 是 |
| 30 | GET | `/life-plans/{id}` | 方案详情 | 是 |
| 31 | POST | `/life-plans` | 新增方案 | 是 |
| 32 | PUT | `/life-plans/{id}` | 更新方案 | 是 |
| 33 | DELETE | `/life-plans/{id}` | 删除方案 | 是 |
| 34 | GET | `/life-advice` | 我的建议列表 | 是 |
| 35 | GET | `/life-advice/{id}` | 建议详情 | 是 |
| 36 | POST | `/life-advice` | 新增建议 | 是 |
| 37 | PUT | `/life-advice/{id}` | 更新建议 | 是 |
| 38 | DELETE | `/life-advice/{id}` | 删除建议 | 是 |
| 39 | POST | `/punch-in` | 新增打卡 | 是 |
| 40 | GET | `/punch-in` | 打卡记录列表 | 是 |
| 41 | GET | `/punch-in/stats` | 打卡统计 | 是 |
| 42 | DELETE | `/punch-in/{id}` | 删除打卡 | 是 |

> `否*`：公开浏览接口，但若携带有效 token 也可以返回个性化数据（收藏状态等）。
> 管理端接口通过 `users.role` 校验，仅 `role = admin` 可访问，否则返回 403（对应前端提示「没有权限执行该操作」）。
> 如需给医生开放文章发布等权限，可将对应接口要求放宽为 `admin` 或 `doctor`。
> Dify 在线咨询相关接口（发起会话、对话、医生 `chat_token` 换取等）**另行单独文档**。

---

## 十一、权限与实现补充约定（四人开发必读）

> 本节内容与同目录《四人开发文档》对应，解决联调与分工中的关键实现约定。

### 11.1 管理端接口权限校验（重要）

文档中标「需管理员权限」的接口（3.3~3.5、4.3~4.5、6.3~6.5）要求 `role = admin`。

后端已实现 `RoleInterceptor` 拦截 `/admin/**` 路径，但上述管理接口当前路径为 `/doctors`、`/articles`、`/diabetes-types`（**不带 `/admin` 前缀**），无法被该拦截器覆盖。**统一约定二选一（推荐方案一）：**

- **方案一（推荐）**：管理端接口路径统一加 `/admin` 前缀：

  | 原路径 | 调整为 |
  | --- | --- |
  | `POST /doctors` | `POST /admin/doctors` |
  | `PUT /doctors/{id}` / `DELETE /doctors/{id}` | `PUT /admin/doctors/{id}` / `DELETE /admin/doctors/{id}` |
  | `POST /articles` | `POST /admin/articles` |
  | `PUT /articles/{id}` / `DELETE /articles/{id}` | `PUT /admin/articles/{id}` / `DELETE /admin/articles/{id}` |
  | `POST /diabetes-types` | `POST /admin/diabetes-types` |
  | `PUT /diabetes-types/{id}` / `DELETE /diabetes-types/{id}` | `PUT /admin/diabetes-types/{id}` / `DELETE /admin/diabetes-types/{id}` |

  由 `RoleInterceptor` 自动完成 403 校验，Controller 中**无需重复判断**；前端调用时使用 `/api/admin/...` 完整路径。

- **方案二**：保持现有路径，在管理接口的 Controller 方法内手动校验：
  `if (!"admin".equals(request.getAttribute("currentRole"))) throw BizException.forbidden("没有权限执行该操作");`

### 11.2 公开浏览接口（`否*`）的可选登录

接口 7/8/12/13/14/22/23 为公开浏览接口（`否*`）。统一约定二选一（推荐方案一）：

- **方案一（推荐）**：完全公开，不校验 token，不返回个性化数据（如收藏状态）。前端收藏按钮等需登录的交互，在用户点击时跳转登录。
- **方案二**：可选登录——有 token 则解析 `userId` 附加个性化数据，无 token 正常返回；需要在 `AuthInterceptor` 中针对这些路径做「可选解析」处理（较复杂，非必需）。

### 11.3 详情接口资源不存在

`GET /doctors/{id}`、`GET /articles/{id}`、`GET /diabetes-types/{id}`、`GET /life-plans/{id}`、`GET /life-advice/{id}`、`DELETE /punch-in/{id}` 等按 id 操作的接口，资源不存在时统一返回 `404`，`message` 如「医生不存在」「文章不存在」「方案不存在」等。

### 11.4 打卡字段枚举

| 字段 | 取值 |
| --- | --- |
| `punchType` | 血糖监测 / 饮食 / 运动 / 作息 |
| `completionStatus` | 已完成 / 未完成 |

### 11.5 Dify 相关接口归属

| 功能 | 归属人员 | 说明 |
| --- | --- | --- |
| 医生在线咨询 | 人员一（首页） | 医生列表 `chatToken` 非空时展示「立即咨询」入口 |
| 智能对话 / AI 风险预测 / AI 方案生成 / 生活状态智能分析 | 人员四（智能助手） | 复用接口 27（风险数据）、31（保存方案）、40/41（打卡数据） |

Dify 接口（发起会话、对话、生成等）**另行单独文档**，前后端按该文档对接。

### 11.6 模块归属（与《四人开发文档》对应）

| 人员 | 模块 | 接口编号 |
| --- | --- | --- |
| 人员一 | 首页 | 7~17、22~26（含管理端） |
| 人员二 | 个人中心 | 1~6、27~28、40~42 |
| 人员三 | 方案定制 + 健康资讯 | 18~21、29~39 |
| 人员四 | 智能助手 + 智能管理 | Dify 接口 + 复用 27 / 31 / 40 / 41 |
