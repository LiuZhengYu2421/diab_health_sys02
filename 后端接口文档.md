# 智糖健康管理平台 - 后端接口文档

> **本文档用途：后端（SpringBoot）接口规范，包含全部 42 个业务接口 + 8 个 Dify 代理接口的定义。**
> 前端启动 / 部署 / 环境配置见同目录 `前端对接说明.md`。
> AI 功能（医师咨询 / 打卡分析 / 风险预测 / 智能助手 / AI 数据助理 / 健康咨询 / 方案定制）对接 Dify 平台，见同目录 `Dify对接文档.md`。
> 数据库建表脚本见同目录 `schema-mysql.sql`。

> 数据库：MySQL（对应 `schema-mysql.sql`）
> 基础路径：开发环境 `/api`（Vite 代理到 `http://localhost:8080`）
> 认证方式：请求头 `Authorization: Bearer <token>`（登录/注册后获取）

---

## 一、通用约定

### 1.1 统一响应格式

所有接口返回以下 JSON 结构：

```json
{
  "code": 200,
  "message": "ok",
  "data": {}
}
```

| code | 含义 | 前端处理 |
| --- | --- | --- |
| 200 或 0 | 成功 | 返回 `data` |
| 400 | 参数错误 | 提示 `message` |
| 401 | 未登录 / token 过期 | 自动清 token 并跳登录页 |
| 403 | 无权限 | 提示「没有权限执行该操作」 |
| 404 | 资源不存在 | 提示 `message` |
| 409 | 冲突（如用户名已注册） | 提示 `message` |
| 500 | 服务器异常 | 提示「服务器异常，请稍后再试」 |

### 1.2 分页参数

| 参数 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `page` | int | 1 | 页码，从 1 开始 |
| `pageSize` | int | 10 | 每页条数 |

分页响应统一返回：

```json
{
  "list": [],
  "total": 100,
  "page": 1,
  "pageSize": 10
}
```

### 1.3 时间格式

所有时间字段统一为 `yyyy-MM-dd HH:mm:ss`（MySQL `DATETIME`）。

---

## 二、认证模块（表 `users`）

> 用户角色由 `users.role` 字段区分：`user`（普通用户，注册默认）/ `doctor`（医生）/ `admin`（管理员）。
> 前端根据 `userInfo.role` 控制管理入口和按钮显隐；管理端接口要求 `role = admin`。

### 2.1 用户登录

- **POST** `/auth/login`
- **无需登录**

请求体：

```json
{ "username": "admin", "password": "admin123" }
```

成功响应 `data`：

```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "userInfo": {
    "id": 1,
    "username": "admin",
    "nickname": "演示用户",
    "avatar": "/img/user_icon.png",
    "desc": "智慧控糖 · 健康生活",
    "role": "admin",
    "createdAt": "2026-08-12"
  }
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `role` | string | `user` / `doctor` / `admin`，注册默认 `user` |

| 错误码 | 场景 |
| --- | --- |
| 400 | 用户名或密码为空 |
| 401 | 用户名或密码错误 |

### 2.2 用户注册

- **POST** `/auth/register`
- **无需登录**

请求体：

```json
{ "username": "zhangsan", "password": "123456", "nickname": "张三" }
```

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 3~20 位，唯一 |
| `password` | string | 是 | 至少 6 位 |
| `nickname` | string | 否 | 默认取 username |

成功响应 `data`：同登录，返回 `{ token, userInfo }`，其中 `role` 固定为 `user`。

| 错误码 | 场景 |
| --- | --- |
| 400 | 参数不合法（用户名过短、密码不足 6 位） |
| 409 | 用户名已存在 |

### 2.3 退出登录

- **POST** `/auth/logout`
- **需登录**（携带 token）

成功响应 `data`：`{}`

### 2.4 获取当前用户信息

- **GET** `/user/info`
- **需登录**

成功响应 `data`：

```json
{
  "id": 1,
  "username": "admin",
  "nickname": "演示用户",
  "avatar": "/img/user_icon.png",
  "desc": "智慧控糖 · 健康生活",
  "role": "admin",
  "createdAt": "2026-08-12"
}
```

> 前端 `userStore` 兼容两种返回：`{ userInfo: {...} }` 或直接 `{...}`，二选一即可。
> 昵称展示优先级：`nickname > username`；头像字段兼容 `avatar` / `avatarUrl`。
> `role` 由服务端从 token 对应的用户读取，前端不可修改。

### 2.5 更新个人信息

- **PUT** `/user/info`
- **需登录**

请求体（只需传要修改的字段）：

```json
{
  "nickname": "新昵称",
  "avatar": "/img/new_avatar.png",
  "desc": "我的控糖宣言"
}
```

成功响应 `data`：更新后的 `userInfo`。

> `role` 不允许用户自行修改，服务端忽略该字段。角色变更仅由管理员操作数据库或专门接口完成。

### 2.6 修改密码

- **PUT** `/user/password`
- **需登录**

请求体：

```json
{ "oldPassword": "123456", "newPassword": "654321" }
```

| 错误码 | 场景 |
| --- | --- |
| 400 | 新密码不足 6 位 |
| 401 | 原密码错误 |

---

## 三、医生资讯模块（表 `doctor_information`）

> 说明：医生**列表与资料管理**走普通 CRUD；「立即咨询」的在线对话部分对接 Dify，见同目录 `Dify对接文档.md`。

### 3.1 医生列表（分页 + 搜索）

- **GET** `/doctors`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `page` | int | 否 | 默认 1 |
| `pageSize` | int | 否 | 默认 10 |
| `keyword` | string | 否 | 按医生姓名模糊搜索 |
| `department` | string | 否 | 按科室精确筛选 |

成功响应 `data`：

```json
{
  "list": [
    {
      "infoId": 1,
      "doctorName": "李建华",
      "department": "内分泌科",
      "title": "主任医师",
      "introduction": "从事糖尿病临床诊疗 25 年...",
      "imageUrl": "/img/doc1.jpg",
      "chatToken": ""
    }
  ],
  "total": 12,
  "page": 1,
  "pageSize": 10
}
```

> `chatToken` 为 Dify 对话所需，未配置时返回空字符串，前端据此决定是否展示「立即咨询」按钮。

### 3.2 医生详情

- **GET** `/doctors/{infoId}`

成功响应 `data`：单个医生对象（字段同上）。

### 3.3 新增医生（管理端）

- **POST** `/admin/doctors`
- **需管理员权限**

请求体：

```json
{
  "doctorName": "李建华",
  "department": "内分泌科",
  "title": "主任医师",
  "introduction": "简介内容",
  "imageUrl": "/img/doc1.jpg",
  "chatToken": ""
}
```

成功响应 `data`：`{ "infoId": 13 }`

> `chatToken` 填写该医生在 Dify 平台对话应用的 API Key（`app-xxx`），用于「立即咨询」在线对话；留空则该医生不展示「立即咨询」按钮，说明见 `Dify对接文档.md` 第 4.4 节。

### 3.4 更新医生（管理端）

- **PUT** `/admin/doctors/{infoId}`
- **需管理员权限**

请求体：同 3.3，字段可选。成功响应 `data`：`{}`

### 3.5 删除医生（管理端）

- **DELETE** `/admin/doctors/{infoId}`
- **需管理员权限**

成功响应 `data`：`{}`

---

## 四、文章科普模块（表 `articles`）

### 4.1 文章列表（分页 + 分类 + 搜索）

- **GET** `/articles`

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `page` | int | 否 | 默认 1 |
| `pageSize` | int | 否 | 默认 10 |
| `category` | string | 否 | 分类（饮食/运动/监测/用药/并发症…） |
| `keyword` | string | 否 | 按标题模糊搜索 |

成功响应 `data`：

```json
{
  "list": [
    {
      "articleId": 1,
      "title": "糖尿病患者秋季饮食指南",
      "coverUrl": "/img/a1.jpg",
      "author": "健康管理师",
      "publishTime": "2026-08-10 09:00:00",
      "content": "文章正文（Markdown 或富文本）",
      "category": "饮食",
      "views": 1200
    }
  ],
  "total": 8,
  "page": 1,
  "pageSize": 10
}
```

### 4.2 文章详情（浏览量 +1）

- **GET** `/articles/{articleId}`

成功响应 `data`：单个文章对象，且服务端 `views` 自动 +1。

### 4.3 新增文章（管理端）

- **POST** `/admin/articles`
- **需管理员权限**

请求体：

```json
{
  "title": "文章标题",
  "coverUrl": "/img/a1.jpg",
  "author": "作者名",
  "publishTime": "2026-08-12 10:00:00",
  "content": "正文内容",
  "category": "饮食"
}
```

成功响应 `data`：`{ "articleId": 9 }`

### 4.4 更新文章（管理端）

- **PUT** `/admin/articles/{articleId}`
- **需管理员权限**

请求体：同 4.3，字段可选。成功响应 `data`：`{}`

### 4.5 删除文章（管理端）

- **DELETE** `/admin/articles/{articleId}`
- **需管理员权限**

成功响应 `data`：`{}`

### 4.6 获取文章分类列表

- **GET** `/articles/categories`

成功响应 `data`：`["饮食", "运动", "监测", "用药", "并发症"]`

---

## 五、文章收藏模块（表 `article_collections`）

### 5.1 收藏文章

- **POST** `/articles/{articleId}/favorite`
- **需登录**

成功响应 `data`：`{}`

> 已收藏时重复调用不报错（幂等），返回成功。

### 5.2 取消收藏

- **DELETE** `/articles/{articleId}/favorite`
- **需登录**

成功响应 `data`：`{}`

### 5.3 我的收藏列表

- **GET** `/articles/favorites`
- **需登录**

请求参数：`page`、`pageSize`（可选）。

成功响应 `data`：

```json
{
  "list": [
    {
      "collectionId": 1,
      "articleId": 1,
      "title": "文章标题",
      "coverUrl": "/img/a1.jpg",
      "author": "健康管理师",
      "publishTime": "2026-08-10 09:00:00",
      "category": "饮食",
      "views": 1200
    }
  ],
  "total": 3,
  "page": 1,
  "pageSize": 10
}
```

### 5.4 查询是否已收藏

- **GET** `/articles/{articleId}/favorite/status`
- **需登录**

成功响应 `data`：`{ "favorited": true }`

---

## 六、用户风险信息模块（表 `user_risk_info`）

> 该表用于风险测评，一个用户对应一条记录。字段为前端已定死的驼峰命名，后端返回时保持一致。

### 6.1 获取我的风险信息

- **GET** `/risk-info`
- **需登录**

成功响应 `data`：

```json
{
  "userId": 1,
  "age": 45,
  "sex": "男",
  "height": 172,
  "weight": 68,
  "familyHistory": "无",
  "waistline": 85.5,
  "systolicPressure": 120,
  "isPregnancy": "否",
  "message": "综合评估结果描述",
  "disease": "2型糖尿病"
}
```

未填写过时返回 `data: null`。

### 6.2 提交 / 更新风险信息

- **POST** `/risk-info`
- **需登录**

请求体：

```json
{
  "age": 45,
  "sex": "男",
  "height": 172,
  "weight": 68,
  "familyHistory": "无",
  "waistline": 85.5,
  "systolicPressure": 120,
  "isPregnancy": "否",
  "message": "综合评估结果描述",
  "disease": "2型糖尿病"
}
```

> 逻辑：当前用户已有记录则覆盖更新，没有则新增。

成功响应 `data`：`{ "userId": 1 }`

---

## 七、生活方案模块（表 `life_plans`）

### 7.1 我的方案列表

- **GET** `/life-plans`
- **需登录**

请求参数：`page`、`pageSize`（可选）；可加 `type`（饮食/运动/其他）筛选。

成功响应 `data`：

```json
{
  "list": [
    {
      "id": 1,
      "userId": 1,
      "type": "饮食",
      "order": 1,
      "time": "08:30",
      "title": "低GI早餐搭配",
      "content": "燕麦 50g + 鸡蛋 1 个 + 无糖豆浆 200ml"
    }
  ],
  "total": 12,
  "page": 1,
  "pageSize": 10
}
```

### 7.2 方案详情

- **GET** `/life-plans/{id}`
- **需登录（只能查自己的）**

成功响应 `data`：单个方案对象。

### 7.3 新增方案

- **POST** `/life-plans`
- **需登录**

请求体：

```json
{
  "type": "饮食",
  "order": 1,
  "time": "08:30",
  "title": "低GI早餐搭配",
  "content": "燕麦 50g + 鸡蛋 1 个"
}
```

> `user_id` 由服务端从 token 解析，前端不传。
> `type` 只能为 `饮食 / 运动 / 其他`。

成功响应 `data`：`{ "id": 13 }`

### 7.4 更新方案

- **PUT** `/life-plans/{id}`
- **需登录（只能改自己的）**

请求体：字段可选。成功响应 `data`：`{}`

### 7.5 删除方案

- **DELETE** `/life-plans/{id}`
- **需登录（只能删自己的）**

成功响应 `data`：`{}`

---

## 八、生活建议模块（表 `life_advice`）

### 8.1 我的建议列表

- **GET** `/life-advice`
- **需登录**

请求参数：`page`、`pageSize`（可选）。

成功响应 `data`：

```json
{
  "list": [
    {
      "id": 1,
      "userId": 1,
      "title": "糖尿病患者秋季饮食指南",
      "tags": "饮食,秋季",
      "content": "建议正文..."
    }
  ],
  "total": 3,
  "page": 1,
  "pageSize": 10
}
```

### 8.2 建议详情

- **GET** `/life-advice/{id}`
- **需登录**

成功响应 `data`：单个建议对象。

### 8.3 新增建议

- **POST** `/life-advice`
- **需登录**

请求体：

```json
{
  "title": "建议标题",
  "tags": "饮食,秋季",
  "content": "建议正文"
}
```

成功响应 `data`：`{ "id": 4 }`

### 8.4 更新建议

- **PUT** `/life-advice/{id}`
- **需登录**

请求体：字段可选。成功响应 `data`：`{}`

### 8.5 删除建议

- **DELETE** `/life-advice/{id}`
- **需登录**

成功响应 `data`：`{}`

---

## 九、打卡记录模块（表 `punch_in`）

### 9.1 新增打卡

- **POST** `/punch-in`
- **需登录**

请求体：

```json
{
  "punchType": "血糖监测",
  "completionStatus": "已完成",
  "message": "空腹血糖 5.6 mmol/L"
}
```

> `punch_time` 由服务端生成当前时间；`user_id` 从 token 解析。

成功响应 `data`：

```json
{
  "id": 1,
  "userId": 1,
  "punchTime": "2026-08-12 08:30:00",
  "punchType": "血糖监测",
  "completionStatus": "已完成",
  "message": "空腹血糖 5.6 mmol/L"
}
```

### 9.2 我的打卡记录列表

- **GET** `/punch-in`
- **需登录**

请求参数：`page`、`pageSize`（可选）；`punchType`（可选，按类型筛选）；`startDate` / `endDate`（可选，按日期范围筛选，格式 `yyyy-MM-dd`）。

成功响应 `data`：

```json
{
  "list": [
    {
      "id": 1,
      "userId": 1,
      "punchTime": "2026-08-12 08:30:00",
      "punchType": "血糖监测",
      "completionStatus": "已完成",
      "message": "空腹血糖 5.6 mmol/L"
    }
  ],
  "total": 26,
  "page": 1,
  "pageSize": 10
}
```

### 9.3 打卡统计

- **GET** `/punch-in/stats`
- **需登录**

成功响应 `data`：

```json
{
  "streak": 12,
  "monthCount": 26,
  "totalCount": 180
}
```

| 字段 | 说明 |
| --- | --- |
| `streak` | 连续打卡天数 |
| `monthCount` | 本月打卡次数 |
| `totalCount` | 累计打卡次数 |

### 9.4 删除打卡记录

- **DELETE** `/punch-in/{id}`
- **需登录（只能删自己的）**

成功响应 `data`：`{}`

> 前端已封装 `POST /punch-in`（`src/api/punchIn.js` 的 `createPunchRecord`），用于「我的 → 打卡记录」新增打卡弹窗。

---

## 十、用户管理模块（表 `users`，管理端）

> 管理端用户列表 / 角色管理 / 软删除 / 恢复，要求 `role = admin`。

### 10.1 用户列表

- **GET** `/admin/users`
- **需管理员权限**

> 返回全部用户（含已删除，`status` 字段标识），可按 `page` / `pageSize` 分页。

成功响应 `data`：

```json
{
  "list": [
    {
      "id": 1,
      "username": "tester01",
      "nickname": "一号用户",
      "role": "user",
      "status": 0,
      "createdAt": "2026-08-12 15:30:55"
    }
  ],
  "total": 20
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `status` | int | `0` 正常 / `1` 已删除（软删除） |

### 10.2 添加用户

- **POST** `/admin/users`
- **需管理员权限**

请求体：

```json
{ "username": "zhangsan", "password": "123456", "nickname": "张三", "role": "user" }
```

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 3~20 位，唯一 |
| `password` | string | 是 | 6~32 位 |
| `nickname` | string | 否 | 默认取 username |
| `role` | string | 是 | `user` / `doctor` / `admin` |

成功响应 `data`：`{ "id": 21 }`

| 错误码 | 场景 |
| --- | --- |
| 400 | 参数不合法 |
| 409 | 用户名已存在 |

### 10.3 修改用户角色

- **PUT** `/admin/users/{id}/role`
- **需管理员权限**

请求体：

```json
{ "role": "doctor" }
```

成功响应 `data`：`{}`

### 10.4 软删除用户

- **DELETE** `/admin/users/{id}`
- **需管理员权限**

> 幂等：已删除用户再次删除仍返回成功。被删除用户无法登录（登录时提示账户异常）。

成功响应 `data`：`{}`

### 10.5 恢复用户

- **PUT** `/admin/users/{id}/restore`
- **需管理员权限**

> 幂等：恢复被软删除用户的登录权限（`status` 置 0）。

成功响应 `data`：`{}`

---

## 十一、健康咨询搜索模块（复用表 `articles`）

> 首页全局搜索（`TeamView.vue`）原为**前端本地匹配**：预先拉取全部咨询标题，再在前端做模糊匹配。现将搜索逻辑下沉到后端：健康咨询文章由 Dify「健康咨询-标签/详情模式」生成后**由后端落库到 `articles` 表**（`category` 分类标记，如 饮食指导 / 运动指南 / 日常习惯 / 糖尿病科普），首页搜索时由 SpringBoot 直接对 `articles` 表做模糊查询。

### 11.1 健康咨询搜索

- **GET** `/articles?keyword=糖尿病`
- **无需登录**（首页公开搜索，携带 token 亦可）
- **直接复用 4.1 文章列表接口**，无需新增接口

请求参数（同上文 4.1）：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `keyword` | string | 是 | 搜索关键词，服务端按标题 `LIKE %keyword%` 模糊匹配 |
| `page` | int | 否 | 默认 1 |
| `pageSize` | int | 否 | 默认 8 |

实现说明：

- SpringBoot 查询 `articles` 表，`title LIKE %keyword%`（忽略大小写）模糊匹配；
- 命中结果按 `publish_time` 倒序返回，返回结构与 4.1 一致（含 `articleId` / `title` / `category` / `coverUrl` 等）。

成功响应 `data`：

```json
{
  "list": [
    {
      "articleId": 3,
      "title": "糖尿病饮食指南",
      "coverUrl": "/img/a3.jpg",
      "author": "AI 健康助手",
      "publishTime": "2026-08-18 09:00:00",
      "content": "文章正文",
      "category": "饮食指导",
      "views": 56
    }
  ],
  "total": 2,
  "page": 1,
  "pageSize": 8
}
```

> 说明：
> - `articles` 表即「文章科普模块」使用的同一张表，健康咨询内容与普通科普文章共用；Dify 生成健康咨询文章后写入 `articles`，即可被本搜索命中。
> - 前端接入：`TeamView.vue` 首页搜索调用 `GET /articles?keyword=xxx&pageSize=8`，替代原有的「拉全量 + 本地匹配」逻辑。

---

## 附录：接口总览

| # | 方法 | 路径 | 说明 | 登录 |
| --- | --- | --- | --- | --- |
| 1 | POST | `/auth/login` | 登录 | 否 |
| 2 | POST | `/auth/register` | 注册 | 否 |
| 3 | POST | `/auth/logout` | 退出登录 | 是 |
| 4 | GET | `/user/info` | 获取用户信息 | 是 |
| 5 | PUT | `/user/info` | 更新个人信息 | 是 |
| 6 | PUT | `/user/password` | 修改密码 | 是 |
| 7 | GET | `/doctors` | 医生列表 | 否* |
| 8 | GET | `/doctors/{id}` | 医生详情 | 否* |
| 9 | POST | `/admin/doctors` | 新增医生 | 管理员 |
| 10 | PUT | `/admin/doctors/{id}` | 更新医生 | 管理员 |
| 11 | DELETE | `/admin/doctors/{id}` | 删除医生 | 管理员 |
| 12 | GET | `/articles` | 文章列表 | 否* |
| 13 | GET | `/articles/{id}` | 文章详情 | 否* |
| 14 | GET | `/articles/categories` | 文章分类 | 否* |
| 15 | POST | `/admin/articles` | 新增文章 | 管理员 |
| 16 | PUT | `/admin/articles/{id}` | 更新文章 | 管理员 |
| 17 | DELETE | `/admin/articles/{id}` | 删除文章 | 管理员 |
| 18 | POST | `/articles/{id}/favorite` | 收藏文章 | 是 |
| 19 | DELETE | `/articles/{id}/favorite` | 取消收藏 | 是 |
| 20 | GET | `/articles/favorites` | 我的收藏列表 | 是 |
| 21 | GET | `/articles/{id}/favorite/status` | 是否已收藏 | 是 |
| 22 | GET | `/risk-info` | 我的风险信息 | 是 |
| 23 | POST | `/risk-info` | 提交/更新风险信息 | 是 |
| 24 | GET | `/life-plans` | 我的方案列表 | 是 |
| 25 | GET | `/life-plans/{id}` | 方案详情 | 是 |
| 26 | POST | `/life-plans` | 新增方案 | 是 |
| 27 | PUT | `/life-plans/{id}` | 更新方案 | 是 |
| 28 | DELETE | `/life-plans/{id}` | 删除方案 | 是 |
| 29 | GET | `/life-advice` | 我的建议列表 | 是 |
| 30 | GET | `/life-advice/{id}` | 建议详情 | 是 |
| 31 | POST | `/life-advice` | 新增建议 | 是 |
| 32 | PUT | `/life-advice/{id}` | 更新建议 | 是 |
| 33 | DELETE | `/life-advice/{id}` | 删除建议 | 是 |
| 34 | POST | `/punch-in` | 新增打卡 | 是 |
| 35 | GET | `/punch-in` | 打卡记录列表 | 是 |
| 36 | GET | `/punch-in/stats` | 打卡统计 | 是 |
| 37 | DELETE | `/punch-in/{id}` | 删除打卡 | 是 |
| 38 | GET | `/admin/users` | 用户列表 | 管理员 |
| 39 | POST | `/admin/users` | 添加用户 | 管理员 |
| 40 | PUT | `/admin/users/{id}/role` | 修改用户角色 | 管理员 |
| 41 | DELETE | `/admin/users/{id}` | 软删除用户 | 管理员 |
| 42 | PUT | `/admin/users/{id}/restore` | 恢复用户 | 管理员 |
| 43 | POST | `/dify/doctor/chat` | Dify 医师咨询对话 | 是 |
| 44 | POST | `/dify/punch/analyze` | Dify 智能打卡分析 | 是 |
| 45 | POST | `/dify/risk/predict` | Dify 糖尿病风险预测 | 是 |
| 46 | POST | `/dify/assistant/chat` | Dify 智能助手对话 | 是 |
| 47 | POST | `/dify/admin/query` | Dify AI 数据助理（管理员） | 管理员 |
| 48 | POST | `/dify/health/tags` | Dify 健康咨询标签模式 | 否 |
| 49 | POST | `/dify/health/detail` | Dify 健康咨询详情模式 | 否 |
| 50 | POST | `/dify/life/scheme` | Dify 方案定制生成 | 是 |

> `否*`：公开浏览接口，但若携带有效 token 也可以返回个性化数据（收藏状态等）。
> 管理端接口通过 `users.role` 校验，仅 `role = admin` 可访问，否则返回 403（对应前端提示「没有权限执行该操作」）。
> 如需给医生开放文章发布等权限，可将对应接口要求放宽为 `admin` 或 `doctor`。
> `#43-#50` 为 **Dify 代理接口**：后端负责接收前端请求、调用 Dify 平台（对话型 / 工作流型应用）并将结果转换为约定格式返回，完整说明见同目录 `Dify对接文档.md`。
> 首页健康咨询搜索不设独立接口，直接复用 `#12 GET /articles?keyword=`（查询 `articles` 表，见第 11 章）。
