# 智糖健康管理平台（Vue 3）· 前端对接说明

> **本文档用途：前端部署、启动、Mock/真实接口切换、认证接口对接约定。**
> 后端全部接口规范见同目录 `后端接口文档.md`。

本项目已由原生 HTML 改造为 **Vue 3 + Vite + Pinia + Vue Router + Axios** 项目，
登录/注册已**完全脱离 Dify**，改为对接自建后端（SpringBoot）。

---

## 一、快速启动

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器（默认 http://localhost:5173）
npm run dev

# 3. 生产构建
npm run build
```

> `VITE_USE_MOCK=true` 时使用本地 Mock 数据，无需后端即可运行演示（登录/注册任意新用户名即可注册，已注册用户可直接登录）。
> **注意：当前 `.env.development` 已配置为 `VITE_USE_MOCK=false`（对接真实后端）**，如需纯前端演示请改回 `true` 并重启 `npm run dev`。

---

## 二、Mock / 真实接口 切换

通过环境变量控制（`.env.development` / `.env.production`）：

| 变量 | 值 | 说明 |
| --- | --- | --- |
| `VITE_USE_MOCK` | `true` | 本地 Mock 模式（localStorage 模拟用户库） |
| `VITE_USE_MOCK` | `false` | 请求真实 SpringBoot 接口 |
| `VITE_API_BASE_URL` | `/api` | 接口前缀 |
| `VITE_API_SERVER_URL` | 空 | 后端完整地址；留空时走 `VITE_API_BASE_URL` + Vite 代理 |

**对接真实后端的步骤：**
1. 启动 SpringBoot 项目（默认端口 `8080`，可通过 `server.port` 修改）。
2. 将 `.env.development` 中 `VITE_USE_MOCK` 改为 `false`。
3. 若后端端口不是 `8080`，同步修改 `vite.config.js` 中 `server.proxy['/api'].target`。
4. 重启 `npm run dev` 即可。

---

## 三、接口约定（请在 SpringBoot 中实现）

前端调用文件：`src/api/auth.js`（请求封装：`src/api/request.js`）

### 1. 统一响应格式

```json
{
  "code": 200,
  "message": "ok",
  "data": {}
}
```

- `code`：`200` 或 `0` 表示成功；`401` 表示未登录/登录过期（前端自动清理并跳转登录页）。
- `data`：业务数据。

完整错误码表（`src/api/request.js` 已统一处理）：

| code | 含义 | 前端处理 |
| --- | --- | --- |
| 200 / 0 | 成功 | 返回 `data` |
| 400 | 参数错误 | 提示 `message` |
| 401 | 未登录 / token 过期 | 自动清 token 并跳登录页（**例外：** `/user/password` 的 401 表示"原密码错误"，仅提示不登出） |
| 403 | 无权限 / 账户被冻结 | 提示 `message`（登录冻结账户提示「账户存在异常请联系管理员」） |
| 404 | 资源不存在 | 提示 `message` |
| 409 | 冲突（如用户名已存在） | 提示 `message` |
| 500 | 服务器异常 | 提示「服务器异常，请稍后再试」 |

### 2. 接口清单（认证 + 个人信息 + 管理端用户账号）

| 方法 | 路径 | 请求体 | 成功返回 data | 说明 |
| --- | --- | --- | --- | --- |
| POST | `/api/auth/login` | `{ "username": "xxx", "password": "xxx" }` | `{ "token": "jwt...", "userInfo": {...} }` | 登录（冻结账户返回 403「账户存在异常请联系管理员」） |
| POST | `/api/auth/register` | `{ "username": "xxx", "password": "xxx", "nickname": "xxx" }` | `{ "token": "jwt...", "userInfo": {...} }` | 注册 |
| POST | `/api/auth/logout` | 无（携带 token） | `{}` | 退出登录 |
| GET | `/api/user/info` | 无（携带 token） | `{ ...userInfo }` | 获取当前用户信息 |
| PUT | `/api/user/info` | `{ "nickname": "xxx", "avatar": "xxx", "desc": "xxx" }`（字段省略不更新） | `{ ...userInfo }` | 更新个人信息 |
| PUT | `/api/user/password` | `{ "oldPassword": "xxx", "newPassword": "xxx" }` | `{}` | 修改密码（原密码错误返回 401，不强制登出） |
| GET | `/api/admin/users` | 无 | `[ {id, username, nickname, role, status, createdAt} ]` | 用户列表（含已删除） |
| POST | `/api/admin/users` | `{ "username": "xxx", "password": "xxx", "nickname": "xxx", "role": "user" }` | `{}` | 添加用户 |
| PUT | `/api/admin/users/{id}/role` | `{ "role": "doctor" }` | `{}` | 修改用户角色 |
| DELETE | `/api/admin/users/{id}` | 无 | `{}` | 软删除用户（幂等） |
| PUT | `/api/admin/users/{id}/restore` | 无 | `{}` | 恢复被软删除用户（恢复登录权限，幂等） |

> 其余业务接口（医生/文章/收藏/风险/方案/建议/打卡，共 42 个）见项目根目录 `后端接口文档.md`。

### 2.1 角色权限控制（配合管理后台）

- 登录/注册返回的 `userInfo.role` 有三种：`user`（默认）/ `doctor` / `admin`
- Pinia 已封装 `userStore.isAdmin`（即 `role === 'admin'`），页面/组件据此控制显隐
- 管理入口（如侧栏"管理后台"菜单）用 `v-if="userStore.isAdmin"` 控制只对管理员显示
- **管理后台为独立顶级路由** `/admin`（自带深色后台布局 `AdminView.vue`），与普通用户前台界面完全区分
- **管理员登录后直接进入管理后台**：`LoginView.vue` 登录成功判断 `userStore.isAdmin` 后 `router.replace('/admin')`；已登录管理员访问 `/login` 时由路由守卫直接重定向到 `/admin`
- 路由守卫已校验 `meta.requiresAdmin`，非 admin 直接访问管理页会自动跳回首页（见 `src/router/index.js`）
- **管理端接口**（新增/修改/删除医生、文章、糖尿病类型、`/admin/users` 用户管理）后端要求 `role = admin`，普通用户调用返回 `code:403`——前端显示「没有权限执行该操作」即可，无需额外处理

```js
// 使用示例（任意页面）
import { useUserStore } from '@/stores/user'
const userStore = useUserStore()
if (userStore.isAdmin) { /* 显示管理操作 */ }
```

> 注意：修改数据库中的角色后需**重新登录**才会生效（token 中的 role 是登录时签发的）。

### 3. userInfo 建议字段

```json
{
  "id": 1,
  "username": "zhang",
  "nickname": "老张",
  "avatar": "/img/user_icon.png",
  "desc": "智慧控糖 · 健康生活",
  "role": "user",
  "createdAt": "2026-08-12"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `role` | string | `user`（默认）/ `doctor` / `admin`，前端据此控制权限入口 |

> 前端展示优先级：`nickname > username > name > userName`；`avatar` 留空时使用默认头像。

### 4. Token 传递

前端会在每次请求的请求头自动携带：

```
Authorization: Bearer <token>
```

SpringBoot 建议使用 JWT 或 Session 校验，`401` 时返回约定格式。

---

## 四、前端接口封装（如需新增业务接口）

`src/api/request.js` 已封装 axios 实例（自动带 token、统一处理错误码），
新增接口时在 `src/api/` 下新建模块即可：

```js
// src/api/health.js
import request from './request'

// 获取健康档案
export function getHealthProfile() {
  return request.get('/user/health-profile')
}

// 提交打卡
export function submitCheckin(data) {
  return request.post('/checkin', data)
}
```

---

## 五、目录结构

```
├── index.html                  # Vite 入口（挂载 #app）
├── vite.config.js              # Vite 配置 + /api 代理到 SpringBoot
├── .env.development            # 开发环境变量（Mock 开关 / 接口地址）
├── .env.production             # 生产环境变量
├── public/                     # 静态资源（css / img / webfonts）
└── src/
    ├── main.js                 # 应用入口
    ├── App.vue                 # 根组件
    ├── router/index.js         # 路由 + 登录守卫
    ├── stores/user.js          # Pinia 用户状态
    ├── api/
    │   ├── request.js          # Axios 封装（token / 错误处理）
    │   ├── auth.js             # 登录/注册/退出/用户信息/改密码（SpringBoot 对接点）
    │   ├── admin.js            # 管理端接口（医生/文章/类型 CRUD + GET/POST /admin/users、改角色、软删除）
    │   └── ...                 # 按四人分工新增模块：doctor.js / article.js / plan.js 等
    ├── utils/
    │   ├── storage.js          # token / 用户信息本地存储
    │   └── alert.js            # 浮动提示
    └── views/
        ├── LoginView.vue       # 登录/注册页（管理员登录后直达 /admin）
        ├── LayoutView.vue      # 主框架（顶栏 + 侧栏 + 内容区，普通用户前台）
        ├── TeamView.vue        # 专业医师团队
        ├── ScienceView.vue     # 健康科普
        ├── MineView.vue        # 个人中心（个人信息管理/编辑资料/修改密码）
        ├── AdminView.vue       # 管理后台主框架（独立顶级路由 /admin，深色后台布局）
        ├── admin/              # 管理后台子面板（四大职责）
        │   ├── DashboardPanel.vue  # 数据总览
        │   ├── DoctorPanel.vue     # 医生团队管理（人员一）
        │   ├── ArticlePanel.vue    # 健康科普文章（人员一）
        │   ├── TypePanel.vue       # 糖尿病类型（人员一）
        │   ├── UserPanel.vue       # 用户账号管理：新增/改角色/软删除（人员二）
        │   ├── PendingPanel.vue    # 建设中占位（人员三/四）
        │   └── panel-style.css     # 面板通用样式
        └── PlaceholderView.vue # 建设中占位页

> 业务接口按《四人开发文档》分模块新增 `src/api/` 下的文件（如 `doctor.js`、`article.js`、`lifeplan.js`、`punchin.js`、`risk.js`），路径与后端接口文档一致（如 `request.get('/doctors', { params })`）。
```
